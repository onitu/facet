h2 Drivers

.table-responsive
  table.table
    thead
      tr
        th Name
        th Driver
        th Options
    tbody
      tr(ng-repeat="driver in drivers")
        td {{driver.name}}
        td
          span.driver {{driver.driver}}
        td {{driver.options}}
